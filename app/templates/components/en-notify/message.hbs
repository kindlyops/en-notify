{{#if message.header}}
  <div class="en-notify-header">
    {{message.header}}

    <div {{action "close"}} class="en-notify-close">
      &times;
    </div>
  </div>
{{/if}}

<div class="en-notify-text">
  {{#if message.text}}
    {{message.text}}
  {{else if message.errors}}
    <ul>
      {{#each message.errors as |error|}}
        <li>
          {{error}}
        </li>
      {{/each}}
    </ul>
  {{/if}}
</div>

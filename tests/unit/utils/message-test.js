import message from '../../../utils/message';
import { module, test } from 'qunit';

module('Unit | Utility | message');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = message();
  assert.ok(result);
});
